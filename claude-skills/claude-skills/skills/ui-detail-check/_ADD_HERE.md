# ui-detail-check 스킬을 여기에 넣어주세요

이 스킬은 이미 계정에 설치돼 있어서 소스 파일이 여기엔 없어요.
아래 중 한 방법으로 이 폴더를 채우면 됩니다.

1) Claude.ai에서 내보낸(export) ui-detail-check 폴더의 내용을 이 폴더에 복사
   - 최소한 `SKILL.md`가 이 폴더 바로 아래 있어야 합니다.
   - references/ 등 보조 파일이 있으면 함께 복사.

2) 이미 다른 컴퓨터의 ~/.claude/skills/ui-detail-check/ 에 있다면 그 폴더를 통째로 복사.

채운 뒤 이 `_ADD_HERE.md` 파일은 지우고 커밋하세요.
(SKILL.md 가 없는 동안에는 install.sh 가 이 폴더를 자동으로 건너뜁니다.)
